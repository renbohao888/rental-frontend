# Railway 部署配置（Nixpacks）
# 构建：npm run build（Vite 生产构建，自动读取 .env.production）
# 启动：vite preview 静态托管 dist 目录，监听 Railway 注入的 PORT
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview -- --host 0.0.0.0 --port $PORT"
